mcp[cli]>=1.0.0
pyyaml>=6.0
# Pinned for deterministic builds. python-frontmatter 1.3.0's dump() writes a
# str, so it needs a text-mode handle (1.1.0 tolerated binary "wb"). The code
# now writes via frontmatter.dumps() + text mode, which is version-safe; the
# pin just stops the unpinned version from drifting again. See
# runner.update_frontmatter / enqueue.py / server.py.
python-frontmatter==1.3.0
